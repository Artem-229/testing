version: "2"

linters:
  default: none
  enable:
    - govet
    - staticcheck
    - unused
    - errcheck
    - rowserrcheck
    - sqlclosecheck
    - noctx
    - ineffassign


formatters:
  enable:
    - goimports

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck